<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title></title>
</head>
<body class="bg-gray-100 overflow-x-hidden">
    <%- include('banner.ejs') %>

    <div class="w-screen h-full flex flex-row justify-center mt-10">

        <div class="w-full max-w-sm bg-white shadow-lg p-4 rounded-xl">
            <a href="/">
                <div class="flex flex-row items-center mb-2 gap-2 hover:underline no-underline cursor-pointer">
                    <img src="/images/back.png" alt="back" class="w-3 h-3"/>
                    <span class="font-bold text-sm">back</span>
                </div>

            </a>
            <div class="space-y-3">
                <div class="bg-gray-100 rounded-xl p-10 w-full max-w-sm max-h-full h-full">
                    <img src="/images/<%= product[0].image %>" class="w-full h-full transform scale-150" alt="img-grid"/>
                </div>

                <div class="-space-y-1">
                    <div class="font-semibold text-sm text-green-400">name</div>
                    <div class="font-bold text-xl"><%= product[0].name %></div>
                </div>

                <div class="-space-y-1">
                    <div class="font-semibold text-sm text-green-400">quantity</div>
                    <div class="font-bold text-xl"><%= product[0].quantity %></div>
                </div>

                <div class="-space-y-1">
                    <div class="font-semibold text-sm text-green-400">type</div>
                    <div class="font-bold text-xl"><%= product[0].type %></div>
                </div>

                <div class="-space-y-1">
                    <div class="font-semibold text-sm text-green-400">description</div>
                    <div class="font-semibold text-sm"><%= product[0].description %></div>
                </div>
    
                <div class="-space-y-1">
                    <div class="font-semibold text-sm text-green-400">source</div>
                    <div class="font-semibold text-xl"><%= product[0].source %></div>
                </div>

                <div class="text-center w-full pt-5">
                    <button id="msgButton" class="text-white font-semibold bg-green-400 p-2 px-6 rounded-md w-full hover:bg-green-300">message</button>
                </div>


            </div>

        </div>

    </div>
    <script>
        const msgbutton = document.querySelector('#msgButton')

        msgbutton.addEventListener('click', () => {
            alert('clicked')
        })

    </script>
</body>
</html>